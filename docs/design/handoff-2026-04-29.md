# Session handoff — 2026-04-29

You are picking up a clean codebase mid-conversation. Read this file first, then read the source-of-truth document at `docs/design/senior-architect-review-2026-04-28.md`.

## Repo state

- Branch: `develop`, working tree clean, in sync with `origin/develop`
- Last commit: `2d53ab7` (`docs: senior architect review 2026-04-28 (post-ianf, pre-action)`)
- Quality: **1705 tests pass, lint clean, mypy clean** across 37 source modules
- `bd ready` returns 3 unrelated open items (mini-projects, an unrelated DSML bug); none block the architect-review work

## What just landed (recent context, NOT what you're working on)

Two consecutive multi-week epics shipped:

1. **`neograph-rnjw`** (commit `d8f5d3d`): introduced `LlmConfig` as a runtime view fixing the `budget_exhausted_message=None` silent-bypass bug.
2. **`neograph-ianf`** (epic): broader cleanup that eliminated untyped `dict[str, Any]` / `Any` / `Callable` boundary crossings:
    - `pej0` — `LlmConfig` promoted from runtime view to IR type carried by `Node.llm_config` and `Construct.llm_config`
    - `l9qb` — `Renderer | None` typing on Node/Construct (Protocol already existed)
    - `2ur4` — typed Protocols for callback slots (`LlmFactory`, `PromptCompiler`, `CostCallback`, Oracle/Loop/raw_fn hooks)
    - `bha9` — Pydantic schema for YAML/JSON spec loader (`_spec_schema.py`)
    - `lxgc` — `RunnableConfig` consistency sweep
    - `mlxg` — `Tool.config` accepted as dynamic with structural guard (`l2vr`)
    - `v9qc` — generics-on-compiled-graph research deferred with explicit re-open conditions
    - 4 follow-ups also closed: `xe1x` (Spec.version field), `xmm8` (typo-rejection at invoke boundary), `l2vr` (Tool.config guard), `gq55` (lint.py docstring clarification)

A multi-agent parallel review (7 reviewer subagents, one per child) confirmed all promises were kept; gaps were captured as the 4 follow-ups above. After ianf closed, a senior-architect-style review was commissioned and persisted as the source-of-truth document below.

## Source of truth for the next phase

**`docs/design/senior-architect-review-2026-04-28.md`** — read it cold, all of it. It contains:

- 5 cited strengths
- 7 weaknesses ordered by severity (W1–W7)
- 5 risks with file:line citations (R1–R5)
- 7 prioritized recommendations (Rec1–Rec7) with effort estimates
- Comparative analysis vs Hamilton, BAML, DSPy, Burr, LCEL+raw LangGraph

Every ticket you file MUST cite specific section numbers from this document. The maintainer explicitly rejected the previous agent's "want me to file these three tickets?" CTA as surface-level — depth matters when acting on substantive work.

## What you are picking up

The user's last instruction was: *"Now we can act on it meaningfully — derive epics and tickets that cite the review's specific weakness/risk numbers as their source of truth, not my off-the-cuff CTA."*

The slicing strategy is **an open question**. Options the user wants to choose between:

- One epic per weakness (W1 … W7) — fine-grained, may over-fragment recommendations that span weaknesses
- One epic per recommendation (Rec1 … Rec7) — already prioritized in the review; recommendations naturally bundle related findings
- Grouped by surface area (e.g., "LLM runtime layer" bundling W1 + Rec1 + R3 + the deferred-import work that touches `_llm.py`)
- Prioritized by load-bearing risk (LangGraph 1.0 readiness epic = W5 + R1)

**Do not pick the strategy unilaterally. Present the tradeoffs to the user and let them choose.**

## What NOT to do

- **Do not re-review the codebase.** It's already reviewed. Read the document.
- **Do not file one-line tickets.** The bar is `pej0` and the ianf children — substantive design body, table of affected sites with file:line, breaking-change analysis, test plan, effort estimate.
- **Do not paraphrase the review.** Tickets quote section headings and reproduce relevant passages so they survive review-document edits.
- **Do not start implementation before the user picks a slicing strategy.** Filing tickets is fine; landing code is not.
- **Do not assume backward compatibility matters.** 0.x library, single consumer (piarch), no migration burden.

## Project conventions (load these into working memory)

- Read `CLAUDE.md` (symlink to `AGENTS.md`) — the source-of-truth for project conventions
- Beads (`bd`) for tracking. NEVER `TodoWrite` or markdown task files. `bd ready` to find work; `bd update <id> --status in_progress` to claim; `bd close <id>` to complete; `bd sync` at session end
- "Landing the plane": session is NOT done until `git push` succeeds. See AGENTS.md "Landing the plane" section
- TDD for bug fixes: failing test first, verify red, fix, verify green
- Three API surfaces parity: `@node` decorator / `ForwardConstruct` / programmatic `Node | Modifier` all converge on the same IR. Any IR-level change must be tested through all three or explicitly justify the exemption
- No emojis (user explicitly rejected them)
- No backwards-compat shims (0.x, single consumer)
- Commit messages: never mention Claude / agents
- Direct, blunt feedback preferred over agreement; the maintainer wants pushback when warranted
- Maintainer is sole author; `piarch` is the only consumer of neograph

## Quick start command

```bash
cd /Users/konst/projects/neograph
git status                                                    # confirm clean
make quality | tail -5                                        # confirm 1705 pass, lint+mypy clean
cat docs/design/senior-architect-review-2026-04-28.md         # read the source of truth
bd ready                                                      # see what's open (none of the 3 hits relate to the review)
```

Then ask the user which slicing strategy they prefer, presenting the tradeoffs from this document. Do not file tickets until they choose.

## If the user is brief or noncommittal

If they say "go ahead" or similar without picking a strategy, default to **one epic per recommendation (Rec1 … Rec7)**, since recommendations are already prioritized in the review and naturally bundle related weaknesses/risks. Cite weakness numbers and risk numbers from the review in each ticket. File the epic + 7 children with the same dependency pattern as `ianf` (epic depends on each child). But name the choice and the reason in your reply so the user can redirect if they had a different shape in mind.

## Heads-up not yet acted on

The post-ianf review surfaced one downstream coordination item not filed as a neograph ticket:

> `LlmConfig.as_factory_kwargs()` now flattens framework defaults (`output_strategy='structured'`, `max_retries=1`) into the factory dict even when the user didn't set them explicitly. piarch's `llm_factory` should be checked: if it filters by key presence (`if 'output_strategy' in cfg:`) rather than reading the value, behavior shifts.

This belongs in piarch's own beads, not neograph's. Do not file it here unless the user asks.

## Open beads tickets unrelated to this work

For awareness only — don't pick these up unless redirected:

- `idqj` (P2 feature) — Mini-project: RFP Response Generator
- `o9bk` (P2 feature) — Mini-project: Incident Response Triage
- `0tid` (P2 bug) — DSML recovery is json_mode/text exclusive
