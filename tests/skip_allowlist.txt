# Skip reasons the release gate tolerates.
#
# EMPTY BY DESIGN. `make release-gate` runs the suite with every extra
# installed, where nothing should need to skip. Each line here is a substring
# matched against a pytest skip reason, and each one records a surface that is
# knowingly NOT exercised by the gate.
#
# This list may only SHRINK. Adding a line is a decision that costs coverage --
# write the reason and the ticket that will remove it again.
#
# (0.7.4 shipped a flaky live test because the Langfuse checks had silently
# skipped; 0.7.7 shipped with 74 mcp tests skipping. Both were invisible in a
# pass count. That is what this file exists to prevent.)
